function lengthOfLongestSubstring(s) {
    // Brute force approach:
    // Create counter
    // Iterate through string and add character to a set
        // If a duplicate is found, remove the last character added, reset the couter to 1
        // If no duplicate, keep counting 
    // 
}

console.log(lengthOfLongestSubstring("abbcab")); // Output: 3