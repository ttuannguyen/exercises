function fourSum(arr, target) {
    arr.sort((a, b) => a - b);

    for (let i = 0; i < arr.length-3; i++) {
        
    }
}

const arr = [2, 7, 4, 0, 9, 5, 1, 3];
const target = 20;
fourSum(arr, target);
