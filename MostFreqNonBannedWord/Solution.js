function find(paragraph, banned) {
    
    let strArray = paragraph.split();
    console.log(strArray);
    
}

const paragraph = "Bob hit a ball, the hit BALL flew far after it was hit.";
const banned = "hit";